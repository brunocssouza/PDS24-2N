let a;
let b;
let resultado;

/* NOT (!) */
a = false;
resultado = !a;

console.log(`!${a} = ${resultado}`);

/* AND (&&) */
a = true;
b = false;
resultado = a && b;

console.log(`${a} && ${b} = ${resultado}`);

/* OR (||) */
a = true;
b = false;
resultado = a || b;

console.log(`${a} || ${b} = ${resultado}`);

/* EQUAL (==) */
a = 1;
b = 2;
resultado = a == b;

console.log(`${a} == ${b} = ${resultado}`);

/* DIFFERENT != */
a = 1;
b = 2;
resultado = a != b;

console.log(`${a} != ${b} = ${resultado}`);

/* GREATER THAN (>) */
a = 1;
b = 2;
resultado = a > b;

console.log(`${a} > ${b} = ${resultado}`);

/* lESSER THAN (<) */
a = 1;
b = 2;
resultado = a < b;

console.log(`${a} < ${b} = ${resultado}`);

/* GREATER OR EQUAL THAN (>=) */
a = 1;
b = 2;
resultado = a >= b;

console.log(`${a} <= ${b} = ${resultado}`);

/* LESSER OR EQUAL THAN (<=) */
a = 1;
b = 2;
resultado = a <= b;

console.log(`${a} <= ${b} = ${resultado}`);






